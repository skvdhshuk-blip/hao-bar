---
version: 1
app: SaneBar
require_standard_runtime_state_matrix: true
runtime_state_matrix:
  upgrade_update:
    why: Customers can only recover from a regression if a fresh install and an upgrade from the prior public build both serve and run the intended version.
    action_ids:
    - startup-wake-appearance-recovery
    - license-about-support-actions
    required_proof_level: full_runtime_completion
    required_evidence_types:
    - mini_runtime
    - state_receipt
    - log
    runner: Scripts/live_zone_smoke.rb plus release_preflight
  cold_launch_relaunch:
    why: New users hit launch, quit, Dock relaunch, and single-instance behavior before they can use any feature.
    action_ids:
    - startup-wake-appearance-recovery
    - status-item-click-routes
    required_proof_level: full_runtime_completion
    required_evidence_types:
    - mini_runtime
    - screenshot
    - state_receipt
    runner: Scripts/live_zone_smoke.rb
  wake_unlock:
    why: Menu bar geometry and app state can drift after wake or unlock even when a normal launch looked correct.
    action_ids:
    - startup-wake-appearance-recovery
    - appearance-customization-actions
    required_proof_level: full_runtime_completion
    required_evidence_types:
    - mini_runtime
    - screenshot
    - log
    runner: Scripts/live_zone_smoke.rb
  display_topology:
    why: External display, notch, and width changes are common customer setups and have caused stale menu-bar placement before.
    action_ids:
    - startup-wake-appearance-recovery
    - browse-icons-search-navigation
    - browse-icons-icon-context-actions
    - appearance-customization-actions
    required_proof_level: full_runtime_completion
    required_evidence_types:
    - mini_runtime
    - screenshot
    - state_receipt
    runner: Scripts/live_zone_smoke.rb
  wake_visible_zone_persistence:
    why: Wake and screen-saver recovery can leave SaneBar anchors healthy while customer-visible third-party icons drift from Visible to Hidden after delayed reconciliation.
    action_ids:
    - startup-wake-appearance-recovery
    - browse-icons-icon-context-actions
    required_proof_level: full_runtime_completion
    required_evidence_types:
    - mini_runtime
    - screenshot
    - log
    runner: Scripts/wake_layout_probe.rb visible-zone snapshots after wake
    required_scenarios:
    - baseline visible icon-zone snapshot before display sleep
    - fresh authoritative icon-zone snapshot at 1s after wake
    - fresh authoritative icon-zone snapshot at 5s after wake
    - fresh authoritative icon-zone snapshot at 15s after wake
    - visible required IDs remain visible and are not moved into Hidden or Always Hidden
    - hidden required IDs remain hidden and are not moved into Visible or Always Hidden
  dynamic_helper_wake_drift:
    why: Helper-owned menu extras such as Lungo can drift across wake/display replay even when stable Apple extras and SaneBar anchors look correct.
    action_ids:
    - startup-wake-appearance-recovery
    - browse-icons-icon-context-actions
    required_proof_level: full_runtime_completion
    required_evidence_types:
    - mini_runtime
    - screenshot
    - log
    - state_receipt
    runner: Scripts/wake_layout_probe.rb helper-specific visible-zone snapshots after wake
    required_scenarios:
    - dynamic helper required IDs are present before wake
    - dynamic helper required IDs remain in intended zones after wake
    - helper-specific Hidden to Visible drift is rejected as a release blocker
  shared_bundle_exact_id_moves:
    why: Move bugs only count as release-proven when the notched MacBook Air drives the real Icon Panel ingress with deterministic SBF fixtures; AppleScript move commands observe a different path.
    action_ids:
    - startup-wake-appearance-recovery
    - browse-icons-icon-context-actions
    required_proof_level: full_runtime_completion
    required_evidence_types:
    - air_runtime
    - log
    - screenshot
    - state_receipt
    runner: manual notched-Mac runtime pass (the historical air_ir_test.sh harness was retired with the sunset)
    required_scenarios:
    - real app ingress used sane_test launch, hotkey, click, right-click menu, and drag tile
    - menu move Hidden to Visible changed authoritative zone
    - menu move Visible to Always Hidden changed authoritative zone
    - menu move Always Hidden to Hidden changed authoritative zone
    - drag move Visible to Hidden changed authoritative zone
    - drag move Hidden to Always Hidden changed authoritative zone
    - drag move Always Hidden to Visible changed authoritative zone
  hover_auto_rehide:
    why: Hover reveal and delayed auto-rehide are timing-sensitive customer flows that source guards and screenshots do not exercise.
    action_ids:
    - control-settings-actions
    - startup-wake-appearance-recovery
    required_proof_level: full_runtime_completion
    required_evidence_types:
    - mini_click
    - mini_runtime
    - log
    - state_receipt
    runner: Mini hover/auto-rehide runtime probe
    required_scenarios:
    - hover reveal opens hidden items
    - leaving the reveal zone auto-rehides after the configured delay
    - repeated hover cycles do not leave stale visible items
  license_clipboard_paste:
    why: A license field can render correctly while failing real paste/focus activation, which blocks paid conversion.
    action_ids:
    - license-about-support-actions
    required_proof_level: full_runtime_completion
    required_evidence_types:
    - mini_click
    - screenshot
    - log
    - state_receipt
    runner: Mini license clipboard paste runtime probe
    required_scenarios:
    - license sheet accepts clipboard paste into the key field
    - Activate uses the pasted value instead of an empty or stale field
    - invalid test key shows a visible validation result without dismissing the sheet
  resource_soak_growth:
    why: A same-candidate adaptive sentinel catches obvious scanner, observer, or helper growth without blocking normal release iteration.
    action_ids:
    - startup-wake-appearance-recovery
    required_proof_level: full_runtime_completion
    required_evidence_types:
    - mini_runtime
    - log
    - state_receipt
    runner: SaneMaster Mini adaptive resource check
    required_scenarios:
    - adaptive Mini resource check passed for this release build
    - average CPU remains within idle budget
    - RSS and physical footprint do not grow beyond the short-soak release budget
    - raw current-build resource soak artifact and log references exist
    - per-sample CPU/RSS/physical footprint trend fields were captured
  basic_pro_mode:
    why: The same customer action must either work in Pro or clearly gate in Basic without pretending the change succeeded.
    action_ids:
    - onboarding-basic-pro-permission-actions
    - pro-basic-gating-actions
    - license-about-support-actions
    required_proof_level: full_runtime_completion
    required_evidence_types:
    - mini_click
    - screenshot
    - log
    runner: Scripts/customer_ui_action_sweep.rb
  support_report_media:
    why: Customers rely on in-app reports when the app is broken, including large videos that cannot travel as normal email attachments.
    action_ids:
    - license-about-support-actions
    - health-repair-rescue-diagnostics
    required_proof_level: full_runtime_completion
    required_evidence_types:
    - mini_click
    - screenshot
    - log
    runner: Scripts/customer_ui_action_sweep.rb support/report media path
actions:
- id: status-item-click-routes
  title: Status item left, right, and option-click routes are live
  surfaces:
  - Menu bar status items
  - SaneBar separator item
  steps:
  - Launch the signed release-like build on the Mini.
  - "Left-click the SaneBar menu bar item with both left-click modes: toggle hidden
    items and open Browse Icons."
  - Option-click the SaneBar menu bar item.
  - Right-click or control-click the SaneBar menu bar item.
  assertions:
  - Left-click toggles hidden icons or opens the configured Browse Icons surface.
  - Option-click always opens the Icon Panel Browse Icons surface.
  - Right-click opens the status menu without toggling hidden icons.
  - The separator stays before the main SaneBar item after each route.
  evidence:
  - Mini click transcript
  - Mini layout snapshot before and after click routes
  required_proof_level: full_runtime_completion
  required_evidence_types:
  - mini_click
  - screenshot
  - fixture
  historical_failure_classes:
  - context_menu_extension_missing
  - duplicate_or_stale_menu_item
  - menu_entry_missing
  - layout_visual_regression
  - state_count_drift
  - activation_noop
  functional_state:
    description: Seeded menu-bar fixture with representative visible, hidden, and
      always-hidden status items and saved SaneBar groups.
    setup_steps:
    - Launch release-like app on the Mini with the standard menu-bar fixture.
    - Restore fixture groups and icon zones before each destructive/move test.
    fixture_paths:
    - Tests/Fixtures/customer-ui/sanebar-menu-zones/
  user_inputs:
  - Launch the signed release-like build on the Mini.
  - '{"Left-click the SaneBar menu bar item with both left-click modes" => "toggle
    hidden items and open Browse Icons."}'
  - Option-click the SaneBar menu bar item.
  expected_outputs:
  - Left-click toggles hidden icons or opens the configured Browse Icons surface.
  - Option-click always opens the Icon Panel Browse Icons surface.
  - Right-click opens the status menu without toggling hidden icons.
- id: status-menu-command-actions
  title: Status menu customer commands open or act from the shipped menu
  surfaces:
  - SaneBar right-click status menu
  steps:
  - Open the status menu from the shipped app.
  - Exercise Browse Icons, Show / Hide Icons, Arrange Now, Help / Repair, Settings,
    About / Report a Bug, What's New when present, and Quit availability.
  - For destructive or externally visible commands, verify the menu item and first
    non-destructive surface, then cancel or close.
  assertions:
  - Every advertised status menu item is present, enabled when applicable, and wired
    to a selector.
  - Browse Icons, Settings, Help / Repair, About / Report a Bug, and What's New when
    present open the intended customer surface.
  - Arrange Now completes without losing the live menu bar anchor.
  - Quit is present but is only invoked in an isolated end-of-run launch/quit check.
  evidence:
  - Mini AX menu transcript
  - Menu selector unit guard
  - Settings, Health, and About screenshots
  required_proof_level: full_runtime_completion
  required_evidence_types:
  - mini_click
  - screenshot
  - fixture
  - log
  historical_failure_classes:
  - context_menu_extension_missing
  - duplicate_or_stale_menu_item
  - menu_entry_missing
  - install_update_packaging
  - persistence_or_relaunch_reset
  - layout_visual_regression
  - pro_basic_gate_drift
  - external_integration_stub
  - state_count_drift
  - docs_promise_drift
  - activation_noop
  functional_state:
    description: Seeded menu-bar fixture with representative visible, hidden, and
      always-hidden status items and saved SaneBar groups.
    setup_steps:
    - Launch release-like app on the Mini with the standard menu-bar fixture.
    - Restore fixture groups and icon zones before each destructive/move test.
    fixture_paths:
    - Tests/Fixtures/customer-ui/sanebar-menu-zones/
  user_inputs:
  - Open the status menu from the shipped app.
  - Exercise Browse Icons, Show / Hide Icons, Arrange Now, Help / Repair, Settings,
    About / Report a Bug, What's New when present, and Quit availability.
  - For destructive or externally visible commands, verify the menu item and first
    non-destructive surface, then cancel or close.
  expected_outputs:
  - Every advertised status menu item is present, enabled when applicable, and wired
    to a selector.
  - Browse Icons, Settings, Help / Repair, About / Report a Bug, and What's New when
    present open the intended customer surface.
  - Arrange Now completes without losing the live menu bar anchor.
- id: dock-menu-command-actions
  title: Dock menu utility commands are reachable
  surfaces:
  - Dock menu
  steps:
  - Enable or use a release-like Dock-visible run.
  - Open the Dock menu.
  - Verify Show All Icons plus shared Settings, License, Check for Updates, About
    / Report a Bug, What's New when present, and Quit commands.
  assertions:
  - Dock menu commands are present and use the shared SaneUI utility actions.
  - Non-destructive commands open the same surfaces as the status menu.
  - Quit is verified in isolation and does not leave duplicate app instances.
  evidence:
  - Mini Dock menu transcript
  - Shared utility menu source guard
  required_proof_level: full_runtime_completion
  required_evidence_types:
  - mini_click
  - screenshot
  - fixture
  - log
  historical_failure_classes:
  - duplicate_or_stale_menu_item
  - menu_entry_missing
  - install_update_packaging
  - persistence_or_relaunch_reset
  - pro_basic_gate_drift
  - external_integration_stub
  - state_count_drift
  - docs_promise_drift
  - activation_noop
  functional_state:
    description: Seeded menu-bar fixture with representative visible, hidden, and
      always-hidden status items and saved SaneBar groups.
    setup_steps:
    - Launch release-like app on the Mini with the standard menu-bar fixture.
    - Restore fixture groups and icon zones before each destructive/move test.
    fixture_paths:
    - Tests/Fixtures/customer-ui/sanebar-menu-zones/
  user_inputs:
  - Enable or use a release-like Dock-visible run.
  - Open the Dock menu.
  - Verify Show All Icons plus shared Settings, License, Check for Updates, About
    / Report a Bug, What's New when present, and Quit commands.
  expected_outputs:
  - Dock menu commands are present and use the shared SaneUI utility actions.
  - Non-destructive commands open the same surfaces as the status menu.
  - Quit is verified in isolation and does not leave duplicate app instances.
- id: browse-icons-search-navigation
  title: Browse Icons opens, searches, navigates, and closes
  surfaces:
  - Browse Icons Icon Panel
  - Search field
  - Category and group tabs
  - Help popover
  steps:
  - Open Browse Icons from the shipped app.
  - Type a query, clear it, switch tabs, use keyboard navigation, and close the panel.
  - Open and close the help popover.
  assertions:
  - The panel opens anchored to live menu bar geometry.
  - Search filters visible rows and clear restores the full list.
  - Arrow and Return navigation activate the highlighted icon or keep focus in the
    panel as designed.
  - Escape or close dismisses the panel and re-arms auto-rehide.
  evidence:
  - Mini Browse Icons screenshot
  - Mini AX and AppleScript transcript
  required_proof_level: full_runtime_completion
  required_evidence_types:
  - mini_click
  - screenshot
  - fixture
  historical_failure_classes:
  - duplicate_or_stale_menu_item
  - menu_entry_missing
  - shortcut_focus_or_dispatch
  - data_loss_or_unexpected_mutation
  - persistence_or_relaunch_reset
  - layout_visual_regression
  - pro_basic_gate_drift
  - state_count_drift
  - docs_promise_drift
  - activation_noop
  functional_state:
    description: Seeded menu-bar fixture with representative visible, hidden, and
      always-hidden status items and saved SaneBar groups.
    setup_steps:
    - Launch release-like app on the Mini with the standard menu-bar fixture.
    - Restore fixture groups and icon zones before each destructive/move test.
    fixture_paths:
    - Tests/Fixtures/customer-ui/sanebar-menu-zones/
  user_inputs:
  - Open Browse Icons from the shipped app.
  - Type a query, clear it, switch tabs, use keyboard navigation, and close the panel.
  - Open and close the help popover.
  expected_outputs:
  - The panel opens anchored to live menu bar geometry.
  - Search filters visible rows and clear restores the full list.
  - Arrow and Return navigation activate the highlighted icon or keep focus in the
    panel as designed.
- id: browse-icons-icon-context-actions
  title: Browse Icons icon tiles execute context actions
  surfaces:
  - Browse Icons icon tiles
  - Icon context menu
  steps:
  - Left-click a representative icon tile.
  - Right-click a representative icon tile.
  - Use Left-Click, Right-Click, Set Hotkey, Copy Icon ID, Move to Visible, Move to
    Hidden, Move to Always Hidden, and Remove from Group where applicable.
  assertions:
  - Left-click and Right-click produce verified activation diagnostics.
  - Copy Icon ID writes the expected identifier.
  - Set Hotkey opens the hotkey recorder without changing unrelated shortcuts.
  - Move commands update the live zone and restore the fixture afterward.
  - Destructive Remove from Group is confirmed or cancelled without corrupting the
    group.
  evidence:
  - Mini icon action transcript
  - Before/after zone snapshot receipt
  required_proof_level: full_runtime_completion
  required_evidence_types:
  - mini_click
  - screenshot
  - fixture
  - log
  historical_failure_classes:
  - context_menu_extension_missing
  - duplicate_or_stale_menu_item
  - menu_entry_missing
  - shortcut_focus_or_dispatch
  - install_update_packaging
  - data_loss_or_unexpected_mutation
  - persistence_or_relaunch_reset
  - layout_visual_regression
  - pro_basic_gate_drift
  - state_count_drift
  - docs_promise_drift
  - activation_noop
  functional_state:
    description: Seeded menu-bar fixture with representative visible, hidden, and
      always-hidden status items and saved SaneBar groups.
    setup_steps:
    - Launch release-like app on the Mini with the standard menu-bar fixture.
    - Restore fixture groups and icon zones before each destructive/move test.
    fixture_paths:
    - Tests/Fixtures/customer-ui/sanebar-menu-zones/
  user_inputs:
  - Left-click a representative icon tile.
  - Right-click a representative icon tile.
  - Use Left-Click, Right-Click, Set Hotkey, Copy Icon ID, Move to Visible, Move to
    Hidden, Move to Always Hidden, and Remove from Group where applicable.
  expected_outputs:
  - Left-click and Right-click produce verified activation diagnostics.
  - Copy Icon ID writes the expected identifier.
  - Set Hotkey opens the hotkey recorder without changing unrelated shortcuts.
- id: second-menu-bar-actions
  title: Second Menu Bar opens, searches, activates, and edits rows
  surfaces:
  - Second Menu Bar window
  - Hidden, Visible, and Always Hidden rows
  - Second Menu Bar toolbar
  steps:
  - Open the Second Menu Bar from the shipped app.
  - Search and clear search.
  - Open Settings and Help from the toolbar, then close them.
  - Left-click, right-click, copy ID, and move representative icons from the row context
    menu.
  - Toggle row visibility chips where available.
  assertions:
  - The Second Menu Bar appears with expected rows and remains open after activation.
  - Search, Help, Settings, and Close controls are live.
  - Row context actions have the same activation and move behavior as Browse Icons.
  - Row visibility controls update the rendered rows and persist.
  evidence:
  - Mini Second Menu Bar screenshot
  - Mini AX and AppleScript transcript
  required_proof_level: full_runtime_completion
  required_evidence_types:
  - mini_click
  - screenshot
  - fixture
  - log
  historical_failure_classes:
  - context_menu_extension_missing
  - duplicate_or_stale_menu_item
  - menu_entry_missing
  - install_update_packaging
  - data_loss_or_unexpected_mutation
  - persistence_or_relaunch_reset
  - layout_visual_regression
  - state_count_drift
  - docs_promise_drift
  - activation_noop
  functional_state:
    description: Seeded menu-bar fixture with representative visible, hidden, and
      always-hidden status items and saved SaneBar groups.
    setup_steps:
    - Launch release-like app on the Mini with the standard menu-bar fixture.
    - Restore fixture groups and icon zones before each destructive/move test.
    fixture_paths:
    - Tests/Fixtures/customer-ui/sanebar-menu-zones/
  user_inputs:
  - Open the Second Menu Bar from the shipped app.
  - Search and clear search.
  - Open Settings and Help from the toolbar, then close them.
  expected_outputs:
  - The Second Menu Bar appears with expected rows and remains open after activation.
  - Search, Help, Settings, and Close controls are live.
  - Row context actions have the same activation and move behavior as Browse Icons.
- id: icon-zone-move-reorder-always-hidden
  title: Icon move, reorder, and Always Hidden behavior is complete
  surfaces:
  - Browse Icons panel
  - Second Menu Bar
  - Icon action menus
  - Always Hidden section
  steps:
  - Select representative exact-ID menu bar fixtures.
  - Move each fixture to Visible, Hidden, and Always Hidden.
  - Reorder an icon within a zone.
  - Reveal hidden icons while an Always Hidden fixture is pinned.
  - Restore the fixture zone.
  assertions:
  - Each move command changes the live zone to the requested zone.
  - Each move remains in the requested zone after the delayed post-move settle window.
  - Reordering does not collapse the separator or move the main SaneBar item.
  - Always Hidden items remain hidden during a normal reveal.
  - Shared-bundle Apple menu extras keep exact identity during verification.
  - Restoring fixtures leaves the Mini menu bar in its prior state.
  evidence:
  - Mini exact-ID move transcript
  - Before/after zone snapshot receipt
  required_proof_level: full_runtime_completion
  required_evidence_types:
  - mini_click
  - screenshot
  - fixture
  historical_failure_classes:
  - duplicate_or_stale_menu_item
  - menu_entry_missing
  - data_loss_or_unexpected_mutation
  - persistence_or_relaunch_reset
  - layout_visual_regression
  - state_count_drift
  - activation_noop
  functional_state:
    description: Seeded menu-bar fixture with representative visible, hidden, and
      always-hidden status items and saved SaneBar groups.
    setup_steps:
    - Launch release-like app on the Mini with the standard menu-bar fixture.
    - Restore fixture groups and icon zones before each destructive/move test.
    fixture_paths:
    - Tests/Fixtures/customer-ui/sanebar-menu-zones/
  user_inputs:
  - Select representative exact-ID menu bar fixtures.
  - Move each fixture to Visible, Hidden, and Always Hidden.
  - Reorder an icon within a zone.
  expected_outputs:
  - Each move command changes the live zone to the requested zone.
  - Moved items stay in the requested zone after delayed reconciliation runs.
  - Reordering does not collapse the separator or move the main SaneBar item.
  - Always Hidden items remain hidden during a normal reveal.
- id: icon-hotkeys-and-groups
  title: Per-icon hotkeys and icon groups behave as customer features
  surfaces:
  - Icon hotkey recorder
  - Smart category tabs
  - Custom groups
  - Group deletion and remove-from-group confirmation
  steps:
  - Record, fire, and clear a representative per-icon hotkey.
  - Switch each smart category tab.
  - Create a custom group, add an icon, remove it, and delete the group.
  assertions:
  - Recorded hotkeys persist and activate the target icon.
  - Smart tabs filter the expected icon population.
  - Custom group create, add, remove, and delete flows persist correctly.
  - Destructive group actions require confirmation or are cancelled cleanly.
  evidence:
  - Mini group and hotkey transcript
  - Persistence unit guard
  required_proof_level: full_runtime_completion
  required_evidence_types:
  - mini_click
  - screenshot
  - fixture
  - log
  historical_failure_classes:
  - shortcut_focus_or_dispatch
  - data_loss_or_unexpected_mutation
  - persistence_or_relaunch_reset
  - layout_visual_regression
  - state_count_drift
  - activation_noop
  functional_state:
    description: Seeded menu-bar fixture with representative visible, hidden, and
      always-hidden status items and saved SaneBar groups.
    setup_steps:
    - Launch release-like app on the Mini with the standard menu-bar fixture.
    - Restore fixture groups and icon zones before each destructive/move test.
    fixture_paths:
    - Tests/Fixtures/customer-ui/sanebar-menu-zones/
  user_inputs:
  - Record, fire, and clear a representative per-icon hotkey.
  - Switch each smart category tab.
  - Create a custom group, add an icon, remove it, and delete the group.
  expected_outputs:
  - Recorded hotkeys persist and activate the target icon.
  - Smart tabs filter the expected icon population.
  - Custom group create, add, remove, and delete flows persist correctly.
- id: settings-shell-tabs-render
  title: Settings shell and every tab render correctly
  surfaces:
  - Settings window
  - Control tab
  - Rules tab
  - Appearance tab
  - Shortcuts tab
  - Health tab
  - About tab
  steps:
  - Open Settings from the shipped app.
  - Select every tab.
  - Capture the window on every tab.
  - Close Settings.
  assertions:
  - Every tab renders without blank, clipped, overlapping, or disabled-by-mistake
    controls.
  - Shared SaneUI Settings, License, and About chrome is used.
  - Closing Settings leaves no stuck modal or orphan window.
  evidence:
  - Mini settings tab screenshot set
  - Mini tab navigation transcript
  required_proof_level: full_runtime_completion
  required_evidence_types:
  - mini_click
  - screenshot
  - fixture
  - log
  historical_failure_classes:
  - shortcut_focus_or_dispatch
  - persistence_or_relaunch_reset
  - layout_visual_regression
  - pro_basic_gate_drift
  - external_integration_stub
  - docs_promise_drift
  - activation_noop
  functional_state:
    description: Seeded menu-bar fixture with representative visible, hidden, and
      always-hidden status items and saved SaneBar groups.
    setup_steps:
    - Launch release-like app on the Mini with the standard menu-bar fixture.
    - Restore fixture groups and icon zones before each destructive/move test.
    fixture_paths:
    - Tests/Fixtures/customer-ui/sanebar-menu-zones/
  user_inputs:
  - Open Settings from the shipped app.
  - Select every tab.
  - Capture the window on every tab.
  expected_outputs:
  - Every tab renders without blank, clipped, overlapping, or disabled-by-mistake
    controls.
  - Shared SaneUI Settings, License, and About chrome is used.
  - Closing Settings leaves no stuck modal or orphan window.
- id: control-settings-actions
  title: Control settings update customer behavior
  surfaces:
  - Settings Control tab
  steps:
  - Switch Browse Icons view between Icon Panel and Second Menu Bar.
  - Change Second Menu Bar row presets and custom rows.
  - Change left-click behavior.
  - Toggle auto-hide, hide-on-app-change, external monitor behavior, hover reveal,
    scroll reveal, drag reveal, hide app menus during inline reveal, and hide-new-items
    behavior.
  - Adjust rehide delays and hover delay.
  - Toggle login item and Dock icon controls where non-destructive.
  - Toggle update auto-check and frequency, then click Check Now.
  assertions:
  - Each control updates persisted settings and the next customer interaction follows
    the new setting.
  - Timing controls clamp to expected ranges.
  - Startup and Dock controls use the shared SaneUI components.
  - Check Now opens the update flow or reports non-release status without hanging.
  evidence:
  - Mini settings action transcript
  - Persistence and update source guards
  required_proof_level: full_runtime_completion
  required_evidence_types:
  - mini_click
  - mini_ax
  - screenshot
  - fixture
  - state_receipt
  historical_failure_classes:
  - duplicate_or_stale_menu_item
  - menu_entry_missing
  - install_update_packaging
  - data_loss_or_unexpected_mutation
  - persistence_or_relaunch_reset
  - layout_visual_regression
  - external_integration_stub
  - state_count_drift
  - docs_promise_drift
  - activation_noop
  functional_state:
    description: Seeded menu-bar fixture with representative visible, hidden, and
      always-hidden status items and saved SaneBar groups.
    setup_steps:
    - Launch release-like app on the Mini with the standard menu-bar fixture.
    - Restore fixture groups and icon zones before each destructive/move test.
    fixture_paths:
    - Tests/Fixtures/customer-ui/sanebar-menu-zones/
  user_inputs:
  - Switch Browse Icons view between Icon Panel and Second Menu Bar.
  - Change Second Menu Bar row presets and custom rows.
  - Change left-click behavior.
  expected_outputs:
  - Each control updates persisted settings and the next customer interaction follows
    the new setting.
  - Timing controls clamp to expected ranges.
  - Startup and Dock controls use the shared SaneUI components.
- id: profiles-save-load-delete-apply
  title: Profiles save, load, delete, and apply across surfaces
  surfaces:
  - Settings Control tab
  - Status menu Profiles submenu
  - App Intent Apply Profile
  steps:
  - Save the current layout as a profile.
  - Load the profile from Settings.
  - Apply the profile from the status menu.
  - Apply the profile through the App Intent path.
  - Delete the profile and cancel at least one destructive delete.
  assertions:
  - Saved profiles persist and are visible in every customer surface.
  - Loading or applying a profile preserves Health Wizard and Layout Rescue state.
  - Deleting a profile removes it from every surface without touching unrelated settings.
  evidence:
  - Mini profile transcript
  - Profile persistence unit guard
  required_proof_level: full_runtime_completion
  required_evidence_types:
  - mini_click
  - screenshot
  - fixture
  historical_failure_classes:
  - duplicate_or_stale_menu_item
  - menu_entry_missing
  - data_loss_or_unexpected_mutation
  - persistence_or_relaunch_reset
  - layout_visual_regression
  - pro_basic_gate_drift
  - state_count_drift
  - activation_noop
  functional_state:
    description: Seeded menu-bar fixture with representative visible, hidden, and
      always-hidden status items and saved SaneBar groups.
    setup_steps:
    - Launch release-like app on the Mini with the standard menu-bar fixture.
    - Restore fixture groups and icon zones before each destructive/move test.
    fixture_paths:
    - Tests/Fixtures/customer-ui/sanebar-menu-zones/
  user_inputs:
  - Save the current layout as a profile.
  - Load the profile from Settings.
  - Apply the profile from the status menu.
  expected_outputs:
  - Saved profiles persist and are visible in every customer surface.
  - Loading or applying a profile preserves Health Wizard and Layout Rescue state.
  - Deleting a profile removes it from every surface without touching unrelated settings.
- id: rules-trigger-actions
  title: Rules triggers can be configured and fired or safely simulated
  surfaces:
  - Settings Rules tab
  steps:
  - Toggle low battery, app launch, schedule, Wi-Fi, Focus, and script triggers.
  - Change thresholds, weekdays, times, selected apps, networks, Focus modes, trigger
    action, and apply-profile choices.
  - Add and remove representative app/network/focus/script entries.
  - Run script-trigger test or simulation where available.
  assertions:
  - Every trigger control persists and validates input.
  - Add/remove controls update the visible lists.
  - Runtime firing or deterministic simulation runs the configured action without
    blocking the app.
  - Pro-only trigger controls show the Pro gate in Basic mode.
  evidence:
  - Mini Rules tab transcript
  - Trigger service unit guards
  required_proof_level: full_runtime_completion
  required_evidence_types:
  - mini_click
  - screenshot
  - fixture
  - log
  historical_failure_classes:
  - install_update_packaging
  - data_loss_or_unexpected_mutation
  - persistence_or_relaunch_reset
  - layout_visual_regression
  - pro_basic_gate_drift
  - state_count_drift
  - activation_noop
  functional_state:
    description: Seeded menu-bar fixture with representative visible, hidden, and
      always-hidden status items and saved SaneBar groups.
    setup_steps:
    - Launch release-like app on the Mini with the standard menu-bar fixture.
    - Restore fixture groups and icon zones before each destructive/move test.
    fixture_paths:
    - Tests/Fixtures/customer-ui/sanebar-menu-zones/
  user_inputs:
  - Toggle low battery, app launch, schedule, Wi-Fi, Focus, and script triggers.
  - Change thresholds, weekdays, times, selected apps, networks, Focus modes, trigger
    action, and apply-profile choices.
  - Add and remove representative app/network/focus/script entries.
  expected_outputs:
  - Every trigger control persists and validates input.
  - Add/remove controls update the visible lists.
  - Runtime firing or deterministic simulation runs the configured action without
    blocking the app.
- id: appearance-customization-actions
  title: Appearance controls update visible output and recover
  surfaces:
  - Settings Appearance tab
  - Menu bar appearance overlay
  - Menu bar icon and dividers
  steps:
  - Change menu bar icon style and custom icon chooser path.
  - Change divider style, visual divider count, and divider look.
  - Toggle custom appearance, translucent background, colors, opacity, shadow, border,
    rounding, and corner radius.
  - Toggle spacing and click-area controls, then restore system defaults.
  - Recreate status items and wake/session observers after appearance changes.
  assertions:
  - Visual controls update persisted settings and rendered output.
  - System spacing changes are reversible and clearly communicate logout requirements.
  - The custom appearance overlay refreshes after status item recovery.
  - No text or controls are clipped after appearance changes.
  evidence:
  - Mini appearance screenshots
  - Overlay recovery transcript
  - MenuBarAppearanceService unit guard
  required_proof_level: full_runtime_completion
  required_evidence_types:
  - mini_click
  - screenshot
  - mini_runtime
  - fixture
  - state_receipt
  historical_failure_classes:
  - duplicate_or_stale_menu_item
  - menu_entry_missing
  - install_update_packaging
  - data_loss_or_unexpected_mutation
  - persistence_or_relaunch_reset
  - layout_visual_regression
  - state_count_drift
  - activation_noop
  functional_state:
    description: Seeded menu-bar fixture with representative visible, hidden, and
      always-hidden status items and saved SaneBar groups.
    setup_steps:
    - Launch release-like app on the Mini with the standard menu-bar fixture.
    - Restore fixture groups and icon zones before each destructive/move test.
    fixture_paths:
    - Tests/Fixtures/customer-ui/sanebar-menu-zones/
  user_inputs:
  - Change menu bar icon style and custom icon chooser path.
  - Change divider style, visual divider count, and divider look.
  - Toggle custom appearance, translucent background, colors, opacity, shadow, border,
    rounding, and corner radius.
  expected_outputs:
  - Visual controls update persisted settings and rendered output.
  - System spacing changes are reversible and clearly communicate logout requirements.
  - The custom appearance overlay refreshes after status item recovery.
- id: shortcuts-and-automation-actions
  title: Shortcuts, URL schemes, App Intents, and AppleScript are equivalent entry
    points
  surfaces:
  - Settings Shortcuts tab
  - Global hotkey recorders
  - Automation command copy buttons
  - URL scheme
  - App Intents
  - AppleScript dictionary
  steps:
  - Record, fire, and clear global hotkeys for Browse Icons, Show / Hide icons, Show,
    Hide, and Settings.
  - Click every automation Copy button and verify clipboard output.
  - Run sanebar://toggle, show, hide, search, settings, health, and repair.
  - Run App Intents for Toggle, Show, Hide, Apply Profile, and Quick Search.
  - Run AppleScript commands for toggle, show, hide, open/close panels, open/close
    settings, list icons/zones, activate/right-click icon, and move/show/hide icon.
  assertions:
  - Each automation entry point reaches the same behavior as the UI action.
  - Copy buttons copy exact documented commands.
  - Auth and Pro gates return explicit errors instead of silently succeeding.
  - AppleScript read commands do not deadlock the main thread.
  evidence:
  - Mini automation transcript
  - AppleScript and App Intent source guards
  required_proof_level: full_runtime_completion
  required_evidence_types:
  - mini_click
  - screenshot
  - fixture
  - log
  historical_failure_classes:
  - context_menu_extension_missing
  - duplicate_or_stale_menu_item
  - menu_entry_missing
  - shortcut_focus_or_dispatch
  - hardware_or_tcc_runtime
  - data_loss_or_unexpected_mutation
  - persistence_or_relaunch_reset
  - layout_visual_regression
  - pro_basic_gate_drift
  - state_count_drift
  - docs_promise_drift
  - activation_noop
  functional_state:
    description: Seeded menu-bar fixture with representative visible, hidden, and
      always-hidden status items and saved SaneBar groups.
    setup_steps:
    - Launch release-like app on the Mini with the standard menu-bar fixture.
    - Restore fixture groups and icon zones before each destructive/move test.
    fixture_paths:
    - Tests/Fixtures/customer-ui/sanebar-menu-zones/
  user_inputs:
  - Record, fire, and clear global hotkeys for Browse Icons, Show / Hide icons, Show,
    Hide, and Settings.
  - Click every automation Copy button and verify clipboard output.
  - Run sanebar://toggle, show, hide, search, settings, health, and repair.
  expected_outputs:
  - Each automation entry point reaches the same behavior as the UI action.
  - Copy buttons copy exact documented commands.
  - Auth and Pro gates return explicit errors instead of silently succeeding.
- id: health-repair-rescue-diagnostics
  title: Health, repair, rescue, and diagnostics actions work
  surfaces:
  - Settings Health tab
  - First-run Health Wizard
  - Help / Repair status menu item
  steps:
  - Open Health from Settings and Help / Repair.
  - Click Open Accessibility, Fix Geometry, Fix SaneBar Items, Stability/Live layout
    mode controls, Arrange Now, Save Current Layout, Restore Last Good Layout, and
    Copy Report.
  - Exercise the first-run Health Wizard path.
  assertions:
  - Permission buttons open System Settings without hanging SaneBar.
  - Repair and Arrange Now refresh menu bar layout and keep SaneBar anchors valid.
  - Layout Rescue create/restore persists and restores a valid snapshot.
  - Copy Report places diagnostics text on the clipboard.
  evidence:
  - Mini Health tab transcript
  - Layout Rescue unit guard
  required_proof_level: full_runtime_completion
  required_evidence_types:
  - mini_click
  - screenshot
  - fixture
  - log
  historical_failure_classes:
  - duplicate_or_stale_menu_item
  - menu_entry_missing
  - hardware_or_tcc_runtime
  - permission_recovery_dead_end
  - install_update_packaging
  - data_loss_or_unexpected_mutation
  - persistence_or_relaunch_reset
  - layout_visual_regression
  - state_count_drift
  - docs_promise_drift
  - activation_noop
  functional_state:
    description: Seeded menu-bar fixture with representative visible, hidden, and
      always-hidden status items and saved SaneBar groups.
    setup_steps:
    - Launch release-like app on the Mini with the standard menu-bar fixture.
    - Restore fixture groups and icon zones before each destructive/move test.
    fixture_paths:
    - Tests/Fixtures/customer-ui/sanebar-menu-zones/
  user_inputs:
  - Open Health from Settings and Help / Repair.
  - Click Open Accessibility, Fix Geometry, Fix SaneBar Items, Stability/Live layout
    mode controls, Arrange Now, Save Current Layout, Restore Last Good Layout, and
    Copy Report.
  - Exercise the first-run Health Wizard path.
  expected_outputs:
  - Permission buttons open System Settings without hanging SaneBar.
  - Repair and Arrange Now refresh menu bar layout and keep SaneBar anchors valid.
  - Layout Rescue create/restore persists and restores a valid snapshot.
- id: data-import-export-reset-actions
  title: Export, import, migration, and reset flows are safe
  surfaces:
  - Settings Control tab Data section
  - Import preview sheet
  - Reset confirmation alert
  steps:
  - Click Export Settings and write a readable settings file to a temporary location.
  - Import exported settings and cancel at least one import preview.
  - Click Import Bartender and Import Ice with representative fixtures.
  - Open Reset to Defaults, cancel it, then run an isolated reset/restore scenario.
  assertions:
  - Export Settings writes a readable settings file.
  - Import preview shows expected differences and creates a rollback point before
    applying.
  - Import Bartender and Import Ice preserve local wizard and rescue state.
  - Reset confirmation is required and reset recreates live status items safely.
  evidence:
  - Mini import/export transcript
  - Import and reset unit guards
  required_proof_level: full_runtime_completion
  required_evidence_types:
  - mini_click
  - screenshot
  - fixture
  historical_failure_classes:
  - menu_entry_missing
  - data_loss_or_unexpected_mutation
  - persistence_or_relaunch_reset
  - layout_visual_regression
  - state_count_drift
  - activation_noop
  functional_state:
    description: Seeded menu-bar fixture with representative visible, hidden, and
      always-hidden status items and saved SaneBar groups.
    setup_steps:
    - Launch release-like app on the Mini with the standard menu-bar fixture.
    - Restore fixture groups and icon zones before each destructive/move test.
    fixture_paths:
    - Tests/Fixtures/customer-ui/sanebar-menu-zones/
  user_inputs:
  - Click Export Settings and write a readable settings file to a temporary location.
  - Import exported settings and cancel at least one import preview.
  - Click Import Bartender and Import Ice with representative fixtures.
  expected_outputs:
  - Export Settings writes a readable settings file.
  - Import preview shows expected differences and creates a rollback point before
    applying.
  - Import Bartender and Import Ice preserve local wizard and rescue state.
- id: onboarding-basic-pro-permission-actions
  title: Onboarding covers donation support, permissions, import, and existing-customer
    paths
  surfaces:
  - First-launch onboarding window
  - Donation support panel
  - Accessibility step
  - Competitor import step
  steps:
  - Run a clean-state first launch.
  - Navigate Back, Next, and Get Started.
  - Click Donate without completing an external donation.
  - Click Import Layout or Import Settings for Bartender and Ice paths.
  - Click Open Accessibility Settings.
  assertions:
  - Every onboarding button is visible and reachable in the fixed window.
  - Accessibility and import flows explain the required action and return safely.
  - Donation support opens the expected external path but SaneBar remains free and
    usable when it is dismissed.
  - Completing onboarding shows the Health Wizard when required.
  evidence:
  - Mini clean-state onboarding transcript
  - Onboarding screenshot set
  required_proof_level: full_runtime_completion
  required_evidence_types:
  - mini_click
  - screenshot
  - fixture
  - log
  historical_failure_classes:
  - hardware_or_tcc_runtime
  - permission_recovery_dead_end
  - data_loss_or_unexpected_mutation
  - persistence_or_relaunch_reset
  - layout_visual_regression
  - pro_basic_gate_drift
  - external_integration_stub
  - state_count_drift
  - activation_noop
  functional_state:
    description: Seeded menu-bar fixture with representative visible, hidden, and
      always-hidden status items and saved SaneBar groups.
    setup_steps:
    - Launch release-like app on the Mini with the standard menu-bar fixture.
    - Restore fixture groups and icon zones before each destructive/move test.
    fixture_paths:
    - Tests/Fixtures/customer-ui/sanebar-menu-zones/
  user_inputs:
  - Run a clean-state first launch.
  - Navigate Back, Next, and Get Started.
  - Click Donate without completing an external donation.
  expected_outputs:
  - Every onboarding button is visible and reachable in the fixed window.
  - Accessibility and import flows explain the required action and return safely.
  - Donation support opens the expected external path but does not gate the app.
- id: license-about-support-actions
  title: License, About, support, and external links are live and cancelable
  surfaces:
  - About tab
  - Pro upsell window
  - Feedback/report bug sheet
  steps:
  - Open License from Settings, status menu, Dock menu, and Pro gates.
  - Click Unlock Pro, Restore Purchases, Enter License Key, Activate, Cancel, and
    Deactivate where applicable without completing a real purchase.
  - Open About, Licenses, GitHub, View Issues, Report a Bug, add/remove feedback attachments,
    copy report, and cancel.
  assertions:
  - License state and Basic/Pro gates agree across all surfaces.
  - Purchase and restore flows open the expected system or checkout path without double-submitting.
  - License activation handles empty/invalid input with visible errors and valid test
    keys in test mode.
  - About and Report a Bug flows use shared SaneUI and never expose stale mailto paths.
  evidence:
  - Mini license/about/support transcript
  - SaneUI shared surface source guard
  required_proof_level: full_runtime_completion
  required_evidence_types:
  - mini_click
  - screenshot
  - fixture
  - support_report
  historical_failure_classes:
  - duplicate_or_stale_menu_item
  - menu_entry_missing
  - data_loss_or_unexpected_mutation
  - persistence_or_relaunch_reset
  - layout_visual_regression
  - pro_basic_gate_drift
  - external_integration_stub
  - state_count_drift
  - docs_promise_drift
  - activation_noop
  functional_state:
    description: Seeded menu-bar fixture with representative visible, hidden, and
      always-hidden status items and saved SaneBar groups.
    setup_steps:
    - Launch release-like app on the Mini with the standard menu-bar fixture.
    - Restore fixture groups and icon zones before each destructive/move test.
    fixture_paths:
    - Tests/Fixtures/customer-ui/sanebar-menu-zones/
  user_inputs:
  - Open License from Settings, status menu, Dock menu, and Pro gates.
  - Click Unlock Pro, Restore Purchases, Enter License Key, Activate, Cancel, and
    Deactivate where applicable without completing a real purchase.
  - Open About, Licenses, GitHub, View Issues, Report a Bug, add/remove feedback attachments,
    copy report, and cancel.
  expected_outputs:
  - License state and Basic/Pro gates agree across all surfaces.
  - Purchase and restore flows open the expected system or checkout path without double-submitting.
  - License activation handles empty/invalid input with visible errors and valid test
    keys in test mode.
- id: pro-basic-gating-actions
  title: Basic and Pro boundaries are enforced at every customer action
  surfaces:
  - Browse Icons
  - Second Menu Bar
  - Settings
  - Onboarding
  - Automation entry points
  steps:
  - Run the action matrix in Basic mode.
  - Run Pro-only move, Always Hidden, custom groups, rules, import/export, appearance,
    shortcuts, and automation actions.
  - Switch to Pro test mode and repeat the same actions.
  assertions:
  - Basic mode can browse and click icons but cannot silently perform Pro-only changes.
  - Pro-only UI opens the Pro gate or returns a clear automation error in Basic mode.
  - Pro mode performs the action and persists the result.
  - Counts, labels, and enabled states never imply a Pro action succeeded in Basic
    mode.
  evidence:
  - Mini Basic and Pro transcript
  - License mode receipt
  required_proof_level: full_runtime_completion
  required_evidence_types:
  - mini_click
  - screenshot
  - fixture
  - log
  historical_failure_classes:
  - duplicate_or_stale_menu_item
  - menu_entry_missing
  - shortcut_focus_or_dispatch
  - hardware_or_tcc_runtime
  - permission_recovery_dead_end
  - data_loss_or_unexpected_mutation
  - persistence_or_relaunch_reset
  - layout_visual_regression
  - pro_basic_gate_drift
  - state_count_drift
  - activation_noop
  functional_state:
    description: Seeded menu-bar fixture with representative visible, hidden, and
      always-hidden status items and saved SaneBar groups.
    setup_steps:
    - Launch release-like app on the Mini with the standard menu-bar fixture.
    - Restore fixture groups and icon zones before each destructive/move test.
    fixture_paths:
    - Tests/Fixtures/customer-ui/sanebar-menu-zones/
  user_inputs:
  - Run the action matrix in Basic mode.
  - Run Pro-only move, Always Hidden, custom groups, rules, import/export, appearance,
    shortcuts, and automation actions.
  - Switch to Pro test mode and repeat the same actions.
  expected_outputs:
  - Basic mode can browse and click icons but cannot silently perform Pro-only changes.
  - Pro-only UI opens the Pro gate or returns a clear automation error in Basic mode.
  - Pro mode performs the action and persists the result.
- id: startup-wake-appearance-recovery
  title: Startup, wake, and appearance recovery preserve customer-visible layout
  surfaces:
  - App launch
  - Display wake recovery
  - Menu bar status items
  - Custom menu bar appearance overlay
  steps:
  - Run the startup layout probe against the signed release-like build.
  - Recreate status items during runtime smoke.
  - Run wake/session/display recovery observers.
  - Confirm overlay refresh after status item recovery.
  assertions:
  - Current display backup beats stale ordinal seeds on startup.
  - currentHost visibility overrides are cleared when needed.
  - The app restores intended layout after wake or session activation.
  - The custom appearance overlay stays tied to live or cached right-side anchors.
  - The app returns to idle within CPU and memory budgets.
  evidence:
  - Mini startup layout probe artifact
  - Mini release runtime smoke transcript
  - Mini resource watchdog receipt
  required_proof_level: full_runtime_completion
  required_evidence_types:
  - mini_runtime
  - screenshot
  - fixture
  - state_receipt
  - log
  historical_failure_classes:
  - duplicate_or_stale_menu_item
  - menu_entry_missing
  - install_update_packaging
  - data_loss_or_unexpected_mutation
  - persistence_or_relaunch_reset
  - layout_visual_regression
  - pro_basic_gate_drift
  - state_count_drift
  - activation_noop
  functional_state:
    description: Seeded menu-bar fixture with representative visible, hidden, and
      always-hidden status items and saved SaneBar groups.
    setup_steps:
    - Launch release-like app on the Mini with the standard menu-bar fixture.
    - Restore fixture groups and icon zones before each destructive/move test.
    fixture_paths:
    - Tests/Fixtures/customer-ui/sanebar-menu-zones/
  user_inputs:
  - Run the startup layout probe against the signed release-like build.
  - Recreate status items during runtime smoke.
  - Run wake/session/display recovery observers.
  expected_outputs:
  - Current display backup beats stale ordinal seeds on startup.
  - currentHost visibility overrides are cleared when needed.
  - The app restores intended layout after wake or session activation.
